/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hide the Next.js development indicator in the browser.
  devIndicators: false,
  // GitHub Pages deployment settings
  output: "export",
  basePath: "/portfolio",
};

export default nextConfig;